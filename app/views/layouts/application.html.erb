<!DOCTYPE html>
<html>
  <head>
    <title>Airbnb | Vacation rentals, cabins, beach houses, & more</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag asset_path("airbnb-favicon.ico") %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header class=" w-full bg-white z-10 shadow-sm nav-header" data-controller="header">
      <%# render "components/navbar/header" %>
      <%= render "header/navbar" %>
    </header>
    <div class="pb-20 pt-28 mt-10 mt-[header-height] px-10">
      <%= render partial: "shared/notifications", locals: { notice_message: flash[:notice], alert_message: flash[:alert] } %>
      <%= yield %>
    </div>
  </body>
  <script src="https://unpkg.com/lucide@latest"></script>
  <script>
    lucide.createIcons();
  </script>
</body>
</html>
